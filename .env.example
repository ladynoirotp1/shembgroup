# Supabase (required for login/register)
# Create a project at https://supabase.com/dashboard
# Then: Project Settings → API → Project URL and anon public key
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key (server-only). Required for admin actions when owner logs in via /owner-login.
# Supabase: Project Settings → API → service_role key (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# In Supabase: Authentication → URL Configuration, add:
#   Redirect URLs: http://localhost:3000/auth/callback
#   (and your production URL, e.g. https://yoursite.vercel.app/auth/callback)

# Owner login (dedicated /owner-login page; not Supabase registration)
# Set these and use /owner-login to access the admin dashboard.
OWNER_EMAIL=owner@example.com
OWNER_PASSWORD=your-secure-password

# Session cookie signing. Use a long random string (e.g. openssl rand -hex 32).
OWNER_SESSION_SECRET=your-session-secret-at-least-16-chars